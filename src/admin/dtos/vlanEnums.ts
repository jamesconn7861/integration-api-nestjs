enum VisibilityEnum {
  Visible = 1,
  Hidden = 2,
  Admin = 3,
}

enum DepartmentEnum {
  Integration = 1,
  Depot = 2,
  'Asset Disposistion' = 3,
  Universal = 4,
}

enum ProtectionEnum {
  Unprotected,
  Protected,
}

export { VisibilityEnum, DepartmentEnum, ProtectionEnum };
